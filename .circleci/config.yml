version: 2.1

#executors:
#  ipinfo_env:
#    docker:
#      - image: circleci/python
#    working_directory: ~/workspace

jobs:
  build:
    docker:
      - image: circleci/python
    working_directory: ~/workspace
    steps:
      - checkout

      - run:
        command: sudo pip install ipwhois
      - run:
        command: sudo pip install flask
      - run: 
        command: sudo python flaskapp.py

#workflow:
#  version: 2.1
#  build_test:
#    jobs:
#      - build
#      - test:
#        requires:
#          - build